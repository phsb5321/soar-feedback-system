"use client";

/**
 * AudioToggle component has been removed as audio control is now handled
 * through individual help buttons. Audio cannot be globally disabled
 * to ensure accessibility and user experience consistency.
 */
export function AudioToggle() {
  // Component removed - audio is controlled via help buttons only
  return null;
}
